import React from 'react'
import { WorkData } from '@/types/types'

interface WorkDetailsProps {
    selectedData: WorkData | null;
}

const WorkDetails: React.FC<WorkDetailsProps> = () => {
  return (
    <div>WorkDetails</div>
  )
}

export default WorkDetails