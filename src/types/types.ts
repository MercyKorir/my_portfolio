export interface WorkData {
  title: string;
  organization: string;
  imageName: string;
}

export interface ProjectData {
  title: string;
  description: string;
  imageName: string;
  demoUrl?: string;
}
